# Update 操作说明（当前设计）

- 服务器对 Update 与其他类型一致：只做互斥与排队，不检查引用计数，也无 `UpdateRequiresNoRef` 配置。
- 是否允许有引用时更新、是否需要跳过操作，由 Content 插件在本地通过 `callback` 包自行判断（可结合计数文件/DB）。
- 客户端协议中的 `skip` 字段保留兼容，但服务器不会置为 `true`；业务侧自行决定“做/不做”。

